

import tkinter as tk

# Create the main window
window = tk.Tk()

# Add widgets (buttons, labels, etc.) to the window
# ...
# Add buttons
button1 = tk.Button(window, text="hola")
button1.pack()

button2 = tk.Button(window, text="bello flor")
button2.pack()

# Add a title
window.title("GUI de laura")

# Set the window properties (size, etc.)
# ...

# Start the main event loop
window.mainloop()